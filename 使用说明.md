# 💕 恋爱小站使用说明

刘雨凝 ❤ 洪闻锴 的专属网页。手机微信里打开，全程免费。

> 同步方案说明：原计划的 LeanCloud 已于 2026-01-12 停止新用户注册，本站改用
> **腾讯 EdgeOne Pages 的免费 Blob 存储（强一致模式）**——不用注册第二个账号、
> 不用申请开通任何服务，按第三节用 GitHub 仓库导入部署后，同步自动开启。

---

## 一、本地试用（无同步）

在电脑上进入 `love-page` 文件夹运行：

```
python -m http.server 8787
```

电脑浏览器打开 `http://127.0.0.1:8787` 即可玩（此时是**本地模式**：数值只存在本机）。

想在部署前预览"两台手机同步"效果（可选）：在项目外层目录运行两个端口

```
python mock_edgeone_api.py 8788
python mock_edgeone_api.py 8789
```

然后一台手机开 `http://电脑IP:8788`、另一台开 `http://电脑IP:8789`，就是两个独立的"用户"，数据实时互通（模拟的就是线上云函数）。

## 二、换头像（两种方式都行）

**方式 A（推荐）：页面里直接换**
登录后点**自己的头像**，选照片，自动裁圆压缩，配置好云端后对方手机同步显示。

**方式 B：替换文件**
替换 `assets/avatar-liu.jpg`（刘雨凝）和 `assets/avatar-hong.jpg`（洪闻锴），文件名别改。
⚠️ 页面上传过的头像优先于文件。想清掉上传的头像：EdgeOne 控制台 → KV 存储 → 命名空间 `love` → 删除 `avatar_liu` / `avatar_hong` 两条记录。

## 三、部署上线 + 开启同步（约 15 分钟，全程免费）

主方案走 **GitHub 仓库导入**：不用申请开通任何存储服务（KV 审批问题直接绕开），部署完成同步就是开启状态。

**第 1 步：注册腾讯云账号**
登录/注册腾讯云（需实名），进入 **EdgeOne Pages** 控制台。

**第 2 步：把文件传到 GitHub**
1. 注册/登录 **github.com** → 右上角 **+** → **New repository** → 名字随意（如 `love-page`），选 **Public** → Create
2. 仓库页点 **uploading an existing file**，把 `love-page` 文件夹里的**全部内容**一起拖进去（index.html、functions、assets 等都要）→ 填一句 Commit 信息 → 点 **Commit changes**，等上传完成
3. 以后想改内容（换歌、加话术），在网页上重新上传覆盖同名文件即可

**第 3 步：EdgeOne 导入仓库部署**
1. EdgeOne Pages → **创建项目** → 选 **导入 Git 仓库** → 授权 GitHub 并选中刚建的仓库 → 部署
2. 平台会自动安装 package.json 里的依赖并构建，完成后得到免费网址（形如 `https://xxx.edgeone.app`）

**验证**：浏览器打开 `你的网址/api/state`，看到 `{"ok":true,"state":{...}}` 就成功了。
之后随便哪台手机点按钮，另一台**立刻**自动同步（Blob 强一致模式，没有延迟），还会回放对方的动画和台词。
数据在控制台 **存储 → Blob 存储** 页面可见（命名空间 `love`）；想把数值清零就删除 `couple` 对象。

> 之前"直接上传文件夹"部署的旧项目可以删掉，以后以这个新网址为准。

**备选：直接上传 + KV 存储**
`functions-kv/` 文件夹里存着 KV 版云函数。如果你的 KV 已通过审批、想走"直接上传文件夹"这条路：把 `functions-kv/api` 里的两个文件覆盖 `functions/api/` 里的同名文件再上传；然后控制台 存储 → KV 存储 → 开通（免费 1GB）→ 创建命名空间 `love` → 在项目里绑定命名空间，**变量名必须填 `LOVE_KV`**。注意 KV 有极少数情况最长 1 分钟同步延迟。

**以后改了内容**（换歌、加话术）：重新上传一次文件夹，数值都在云端不会丢。

## 四、在微信里用得顺手的小技巧

- 把网址**发给"文件传输助手"或对方**，从聊天里点开，提示"非微信官方网页"点继续访问
- 手机浏览器打开后 → 菜单 → **添加到主屏幕**，以后像 App 一样点图标进
- 想换电脑本地调试时用局域网 IP（`http://电脑IP:端口`），**不要用 127.0.0.1**（那是指手机自己）；打不开就让防火墙放行端口：
  ```
  netsh advfirewall firewall add rule name="love-page-8787" dir=in action=allow protocol=TCP localport=8787
  ```

## 五、换背景音乐

你已经放好 3 首歌啦（《我们俩》《离开我的依赖》《告白气球》）。以后增删歌就在 `config.js` 的 `MUSIC_LIST` 里改，mp3 放 `assets/` 目录。歌多于 1 首时右上角会出现 `»` 切歌按钮。

## 六、改数值规则 / 说话概率 / 部位 / 话术

- 数值规则 `RULES`、说话概率 `LINE_CHANCE`、部位列表 `PEEK_PARTS`：都在 `config.js`
- 话术库：`app.js` 最顶上的 `LINES`，往列表里加句子就行；`{call}` 自动变"老公/老婆"

## 七、可选：给数据上把锁

担心网址被陌生人猜到乱改？在 `config.js` 里把 `SYNC_PASSCODE` 填上一串只有你们俩知道的字符（比如 `'liu-hong-520'`），同时把 `functions/api/state.js` 和 `functions/api/avatar.js` 顶部的 `PASSCODE` 改成同一串，重新上传。两边口令一致才能同步。

## 八、常见问题

| 问题 | 解决 |
| --- | --- |
| 音乐不响 | 微信禁止自动播放，**点一下屏幕任意位置**就开始；右上角 ♪ 是开关 |
| 打开 `/api/state` 显示 404 | 函数没部署上：确认 `functions` 文件夹一起上传了，重新部署一次 |
| 打开 `/api/state` 显示 404 | 函数没部署上：确认用的是「GitHub 仓库导入」方式，且 `functions` 文件夹已完整上传 |
| `/api/state` 报 credential/environment 错 | Blob 函数需要平台构建注入凭据：必须用 GitHub 导入部署，不能直接上传文件夹 |
| 数值两边不一致 | Blob 强一致方案是即时同步；若换用了 functions-kv 的 KV 方案，极少数情况最长 1 分钟 |
| 想把数值清零重来 | EdgeOne 控制台 → 存储 → Blob 存储 → 命名空间 `love` → 删除 `couple` 对象（删 `avatar_liu`/`avatar_hong` 可清头像） |
| 微信里打不开网址 | 从聊天记录里点链接打开，别直接搜索输入 |

## 九、文件结构

```
love-page/
  index.html          页面结构
  style.css           所有样式（颜色、动画都在这）
  app.js              逻辑（顶部是话术库 LINES）
  config.js           常改的配置（歌单/数值规则/概率/口令）
  package.json        Blob 云函数的依赖声明（部署时平台自动安装，别删）
  functions/
    api/state.js      云函数：读取/更新共享数值（Blob 强一致版）
    api/avatar.js     云函数：头像上传与获取（Blob 强一致版）
  functions-kv/       备用：KV 版云函数（想改用 KV 存储时覆盖 functions/api 同名文件）
  assets/             头像两张 + 背景音乐
```
